# DEPRECATED — GitHub Pages moved to OsEngine.
# Live site: https://robinzgit.github.io/OsEngine/
# Workflow: RobinZGit/OsEngine → .github/workflows/pages.yml
# This file is kept only for history in the archived MultiLogicTradePg repo.

name: Deploy web to GitHub Pages (DEPRECATED — use OsEngine)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Pages moved to OsEngine
        run: |
          echo "GitHub Pages for MultiLogicTradePg now deploys from RobinZGit/OsEngine."
          echo "URL: https://robinzgit.github.io/OsEngine/"
          echo "This repository is archived; do not deploy from here."
          exit 1
